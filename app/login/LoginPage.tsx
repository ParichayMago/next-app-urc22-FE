'use client';
import React, { useState } from 'react';
import LoginCard from './LoginCard';

export default function LoginPage() {
  
  return (
      <main>
        <LoginCard />
      </main>
  );
}
